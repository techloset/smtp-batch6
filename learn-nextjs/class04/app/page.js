

export default function Home() {
  // let userScore = 40;
  // let grade = ""
  // if(userScore >= 90 && userScore <= 100){
  //    grade = "A"
  // }
  // else if(userScore >= 80 && userScore <90){
  //   grade = "B"
  // }
  // else if(userScore >= 70 && userScore <80){
  //   grade = "C"
  // }
  // else if(userScore >= 60 && userScore < 70){
  //   grade = "D"
  // }
  //  else {
  //   grade = "F"
  //  }
  return (

    <div>
     

   
      {/* {
       grade !== "F" ?
        <div> User is passed and grade is {grade}</div>
        :
        <div> User is failed and grade is {grade}</div>
      }
      */}
    
      {/* {isLoggedIn ? (
        <button>
          Logout
        </button>) :
        (<button>
          Login
        </button>
        )
      } */}
    </div>
  )
}
